export const setNavbar = (isNavbar)=>({
   type:"SET_NAVBAR",
   isNavbar
})


export const setSidebar = (isSidebar)=>({
   type:"SET_SIDEBAR",
   isSidebar
})

export const setTab = (activeTab)=>({
   type:"SET_TAB",
   activeTab
})

export const setToast = (isToast)=>({
   type:"SET_TOAST",
   isToast
})

export const setInfo = (isInfo)=>({
   type:"SET_INFO",
   isInfo
})

export const setPeople = (peoples)=>({
   type:"SET_PEOPLES",
   peoples
})

export const setPopOver = (popover)=>({
   type:"SET_POPOVER",
   popover
})

export const setUser = (user)=>({
   type:"SET_USER",
   user
})

export const setDropDown = (userDropDown)=>({
   type:"ACCOUNT_DROPDOWN",
   userDropDown

})